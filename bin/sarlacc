#!/usr/bin/env ruby

require 'rubygems'
require 'bundler/setup'

require 'eventmachine'
require 'lib/sarlacc'

EventMachine.run {
#  EventMachine.add_periodic_timer(1) {
  lister = Sarlacc.new('http://newyork.ucbtheatre.com/classes/rss')
  lister.onfeed do |feed|
    puts "Feed: #{feed.title}"
  end
#  }
}
